#!/bin/bash
# This script will be uploaded separately as it's too long for a single file write
echo "Due to file length constraints, please run this analysis using the existing density_analysis_unet_only_autocontrast.py as a template"
echo "and manually create 3 versions for each architecture, then combine the results."
echo ""
echo "Alternative: Use the existing scripts and run them separately for each architecture:"
echo "1. density_analysis_unet_only.py (for UNet)"
echo "2. Modify for attention_unet"
echo "3. Modify for attention_resunet"
